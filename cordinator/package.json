{
  "name": "hotspotpluscordinator",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./app.js",
    "releaseSandbox":"pkg -t node8-linux-x64  app.js -o hotspotplus-cordinator;git commit -a -m 'sandbox:3.3';git tag -f release ;git push -u origin develop --tags --force",
    "releaseProduction":"pkg -t node8-linux-x64  app.js -o hotspotplus-cordinator;git commit -a -m 'production:3.3';git tag -f release ;git push -u origin master --tags --force",
    "releaseLocalProduction":"pkg -t node8-linux-x64  app.js -o hotspotplus-cordinator;git commit -a -m 'production:latest';git tag -f release ;git push -u origin master --tags --force"
  },
  "dependencies": {
    "cron": "^1.3.0",
    "date-utils": "^1.2.21",
    "hotspotplus-common": "git+https://gitlab-ci-token:yw4Y31Gx9eRZKVH2GCt5@gitlab.com/parmenides/HotspotplusCommon.git#licensed",
    "needle": "^2.2.1",
    "node-rsa": "^1.0.0",
    "nodejs-license-file": "^4.0.0",
    "q": "^1.5.1",
    "redis": "^2.8.0",
    "temp": "^0.8.3"
  }
}
